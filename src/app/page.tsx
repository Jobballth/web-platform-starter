export default function PaddingDemo() {
  return (
    <div className="p-10 space-y-8 bg-gray-100 min-h-screen">
      
      {/* 📦 1. การ์ด: ใช้ p-6 (เหมาหมด) เพื่อให้เนื้อหาไม่ชนขอบ */}
      <div className="bg-white p-6 rounded-lg shadow-md w-64">
        <h2 className="font-bold text-xl mb-2">Card Title</h2>
        <p className="text-gray-600">
          สังเกตว่าตัวหนังสือไม่ชนขอบกล่องเลย เพราะมีไขมัน (Padding) ดันไว้อยู่
        </p>
      </div>

      {/* 🔘 2. ปุ่ม: ใช้สูตร px > py */}
      <div>
        <h3 className="font-bold mb-2">สูตรปุ่มมาตรฐาน</h3>
        {/* py-2 (8px) | px-4 (16px) -> สวยเป๊ะ */}
        <button className="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">
          Click Me
        </button>
      </div>

      {/* 📱 3. กันขอบจอ: ใช้ px-4 ที่ตัวแม่ */}
      {/* เวลาดูบนมือถือ เราไม่อยากให้ตัวหนังสือติดขอบจอเกินไป */}
      <div className="bg-white px-4 py-8 border-l-4 border-blue-500">
        <p>
          Section นี้มีการเว้นขอบซ้ายขวา (px-4) 
          และเว้นหัวท้ายเยอะหน่อย (py-8) ให้ดูโล่งสบายตา
        </p>
      </div>

    </div>
  );
}